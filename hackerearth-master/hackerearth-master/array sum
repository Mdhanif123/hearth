#include <iostream>
using namespace std;

int main() {
	long long int n;;
	cin >> n;
	long int sum = 0;
	for (long long int  i = 0; i < n; ++i) {
		long long int x;
		cin >> x;
		sum +=x;
	}
	cout<<sum;
	return 0;
}
